{
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "nohoist": [
      "catalog-frontend/@testing-library/**"
    ]
  },
  "scripts": {
    "build": "lerna run build"
  },
  "name": "CDK-Construct-Catalog",
  "useWorkspaces": true,
  "devDependencies": {
    "lerna": "^3.20.2"
  }
}
